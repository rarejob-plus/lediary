{
  "name": "lediary-next",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --port 5181",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --port 5181",
    "deploy": "npm run build && firebase deploy --only hosting:lediary-next --project otokichi-app"
  },
  "dependencies": {
    "firebase": "^11.10.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.4.0"
  }
}
